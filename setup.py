from distutils.core import setup
setup(
  name = 'ccfs',
  packages = ['ccfs'], # this must be the same as the name above
  version = '0.2',
  description = 'count character from string lib',
  author = 'zaeval',
  author_email = 'zaeval@kookmin.ac.kr',
  url = 'https://github.com/zaeval/ccfs', # use the URL to the github repo
  download_url = 'https://github.com/zaeval/count_character_from_string/tree/master/ccfs/dist/0.1.tar.gz', # I'll explain this in a second
  keywords = ['count','character','string'], # arbitrary keywords
  classifiers = [],
)